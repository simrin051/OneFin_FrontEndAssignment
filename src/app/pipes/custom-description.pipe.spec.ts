import { CustomDescriptionPipe } from './custom-description.pipe';

describe('CustomDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
